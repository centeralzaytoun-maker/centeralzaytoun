-- ════════════════════════════════════════════════════════
-- إضافة حد أقصى لعدد الموظفين في الباقات
-- شغّل هذا الـ SQL في Supabase SQL Editor
-- ════════════════════════════════════════════════════════

ALTER TABLE packages
  ADD COLUMN IF NOT EXISTS max_staff INTEGER DEFAULT NULL;
-- NULL = مفتوح (غير محدود)
-- رقم = الحد الأقصى للموظفين المسموح بهم في الباقة
