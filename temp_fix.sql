-- ========================================
-- إصلاح مؤقت لمشكلة RLS في middleware
-- ========================================

-- سياسة مؤقتة: تسمح للـ middleware يشوف staff_profiles
CREATE POLICY "Allow middleware to read staff profiles"
ON public.staff_profiles
FOR SELECT
USING (true);  -- ← مؤقتاً بس عشان نشوف المشكلة

-- ========================================
-- ملاحظة: دي سياسة مؤقتة غير آمنة!
-- لازم تشغلها عشان نعرف المشكلة، وبعدين نحذفها
-- ونرجع السياسات الأصلية الآمنة
-- ========================================
